# ShineProCS - UI 开发设计规范文档

**文档版本**: v1.0

**适用框架**: .NET 9.0 + WPF

**UI 库依赖**: ModernWpfUI (主题), HandyControl (控件)

**设计风格**: Windows 11 Fluent Design (半透明云母材质、圆角、响应式)

------

## 1. 界面总体布局 (Layout Architecture)

根据“技能循环引擎”的特性，界面采用 **Dashboard (仪表盘)** 布局，强调信息的实时性和操作的便捷性。

### 1.1 主窗口结构图

Plaintext

```
+---------------------------------------------------------------+
|  TitleBar (系统菜单 | 最小化 | 最大化 | 关闭)             |
+-----------------------------------+---------------------------+
|  A. 侧边导航栏 (Navigation)       |                           |
|                                   |                           |
|  [🏠 概览/监控]                   |  C. 内容区域              |
|  [⚙️ 策略配置]                   |  (Content Region)         |
|  [📊 性能分析]                   |                           |
|  [🛠️ 全局设置]                   |  (通过 Frame 或           |
|                                   |   ContentControl 切换)    |
|                                   |                           |
|                                   |                           |
|-----------------------------------|                           |
|  B. 引擎控制区 (Engine Control)   |                           |
|  [🔴 停止 / 🟢 运行]              |                           |
|  状态: 监听中...                  |                           |
+-----------------------------------+---------------------------+
|  D. 底部状态栏 (StatusBar)                                    |
|  CPU: 1.2% | 内存: 45MB | 延迟: 12ms | 游戏状态: 已连接       |
+---------------------------------------------------------------+
```

### 1.2 区域详细说明

| **区域**        | **控件建议**               | **功能描述**                                           | **对应需求**   |
| --------------- | -------------------------- | ------------------------------------------------------ | -------------- |
| **A. 导航**     | `ModernWpf:NavigationView` | 侧边汉堡菜单，支持展开/收起，图标+文字。               | 1.3 现代化界面 |
| **B. 引擎控制** | `ToggleButton` / `Badge`   | **核心开关**。醒目的圆形按钮，带呼吸灯效果（运行中）。 | 1.3 实时状态   |
| **C. 内容区**   | `Frame`                    | 页面路由容器，展示不同功能模块。                       | -              |
| **D. 状态栏**   | `StatusBar`                | 底部细条，实时显示性能指标（需右对齐）。               | 1.5 性能监控   |

------

## 2. 核心页面设计 (View Specifications)

### 2.1 概览监控页 (DashboardView)

**功能**: 这里的核心是“所见即所得”，用户挂机时主要看这个页面。

- **视觉分块**:
  1. **当前策略卡片**: 显示当前激活的策略（如“千枝→赤芍联动”），配有大的状态指示灯。
  2. **技能状态流 (Skill Timeline)**:
     - *设计*: 横向滚动的流式布局。
     - *元素*: 每个技能是一个方块，显示图标、剩余冷却时间 (倒计时遮罩)、Buff 状态小图标。
     - *动态效果*: 技能释放时有高亮边框闪烁。
  3. **实时日志窗 (Live Log)**:
     - *控件*: `RichTextBox` 或 虚模式 `ListView` (高性能)。
     - *样式*: 黑色半透明背景，Consolas 字体，不同级别的日志（Info/Warn/Error）使用不同颜色。

### 2.2 策略配置页 (StrategyConfigView)

**功能**: 对应需求 FR-001 和 FR-005，用户在这里配置“如果A则B”的逻辑。

- **交互设计**: 既然需求提到“图形化配置”，建议采用 **主从列表** 或 **树形结构**。

- **左侧**: 策略列表（如：默认策略、高压治疗策略、输出策略）。

- **右侧**: 规则编辑器（卡片式列表）。

  - **规则卡片示例**:

    Plaintext

    ```
    [ 规则 1: 赤芍寒香前置检查 ]
    ------------------------------------------------
    条件 (If): [并没有] [Buff: 千枝状态]
    动作 (Then): [释放] [技能: 千枝绽蕊]
    优先级: [高]
    ```

- **控件建议**: 使用 `HandyControl:StepBar` (步骤条) 或者 `Expander` (折叠面板) 来组织复杂的依赖关系。

### 2.3 性能分析页 (PerformanceView)

**功能**: 对应需求 1.5，展示图表。

- **控件**: 建议引入 `LiveCharts2` (WPF版)。
- **图表内容**:
  - **折线图**: 最近 60 秒的 识别延迟 (ms)。
  - **饼图**: 技能释放分布统计。

------

## 3. UI 视觉规范 (Design System)

为了符合“Windows 11 设计语言” (1.3)，请严格执行以下 XAML 样式规范。

### 3.1 配色方案 (Theme)

支持深色 (Dark) 和 浅色 (Light) 切换，默认深色。

| **颜色语义**   | **Dark Mode Hex**    | **Light Mode Hex**  | **用途**             |
| -------------- | -------------------- | ------------------- | -------------------- |
| **Primary**    | `#FF4CC2FF` (荧光蓝) | `#FF0078D7` (Win蓝) | 主按钮、高亮文字     |
| **Background** | `#FF202020` (深灰)   | `#FFF3F3F3` (云白)  | 窗口背景             |
| **Surface**    | `#FF2D2D30`          | `#FFFFFFFF`         | 卡片、面板背景       |
| **Success**    | `#FF6CCB5F`          | `#FF107C10`         | 运行中、识别成功     |
| **Warning**    | `#FFFFD300`          | `#FF9A7600`         | 冷却中、依赖缺失     |
| **Error**      | `#FFFF4C4C`          | `#FFE81123`         | 识别失败、游戏未连接 |

### 3.2 字体与排版

- **主要字体**: `Segoe UI Variable` (Win11 默认), 备用 `Microsoft YaHei UI`。
- **代码/日志**: `Cascadia Code` 或 `Consolas`。
- **字号**:
  - 标题: 24px SemiBold
  - 卡片标题: 16px Bold
  - 正文: 14px Regular
  - 辅助文字: 12px Grey

### 3.3 控件样式特征

- **圆角 (CornerRadius)**:
  - 窗口/主容器: `8px`
  - 按钮/输入框: `4px`
- **材质 (Material)**:
  - 背景启用 `Mica` (云母) 效果（ModernWpf 支持）。
  - 弹窗/菜单启用 `Acrylic` (亚克力) 模糊效果。

------

## 4. MVVM 交互规范 (Interaction Specs)

虽然不写后端，但 UI 必须定义好与 ViewModel 的数据绑定契约。

### 4.1 数据绑定模式

由于 1.1 需求中提到“响应时间 < 50ms”，UI 刷新必须高效：

- **高频数据** (如冷却倒计时、性能图表): 使用 `x:Bind` (编译时绑定) 且模式为 `OneWay`，避免双向绑定的开销。
- **列表数据** (日志): 必须使用 `ObservableCollection`，并且在后台线程更新时通过 `BindingOperations.EnableCollectionSynchronization` 跨线程更新，防止 UI 卡死。

### 4.2 命令定义 (Commands)

UI 按钮仅绑定命令，不写 Click 事件。

- `StartEngineCommand`: 切换 toggle 状态。
- `ImportConfigCommand`: 打开文件选择器。
- `EditRuleCommand`: 传入 RuleID，打开模态窗口。

------

## 5. 特殊功能 UI 实现方案

### 5.1 系统托盘 (System Tray)

- **需求**: 1.3 最小化到托盘。
- **实现**: 使用 `Hardcodet.NotifyIcon.Wpf` 库。
- **交互**:
  - 点击关闭按钮 -> 最小化到托盘，而非退出。
  - 托盘右键菜单 -> [显示主界面, 暂停引擎, 退出程序]。
  - 托盘图标状态 -> 灰色(停止) / 绿色(运行中)。

### 5.2 悬浮窗 (Overlay Mode)

- **需求**: 游戏全屏时查看状态。
- **设计**: 一个独立的、无边框、背景完全透明 (`AllowsTransparency="True"`) 的小窗口。
- **位置**: 默认屏幕右下角，支持拖拽。
- **内容**: 仅显示 `[运行状态]` 和 `[当前队列下一个技能]`。
- **属性**: `Topmost="True"` (置顶), `ShowInTaskbar="False"`.

------

## 6. 开发资源清单

为了开始 UI 开发，你需要准备以下资源：

1. **图标库**: 建议使用 `Segoe Fluent Icons` 字体图标（Windows 11 原生），或者导出 SVG 路径。
   - *Play*: ``
   - *Pause*: ``
   - *Settings*: ``
   - *Flash (Buff)*: ``
2. **NuGet 包**:
   - `ModernWpfUI`
   - `HandyControl`
   - `CommunityToolkit.Mvvm` (用于绑定)
   - `LiveChartsCore.SkiaSharpView.WPF` (可选，用于图表)

