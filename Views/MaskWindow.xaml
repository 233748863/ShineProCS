<Window x:Class="ShineProCS.Views.MaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:ShineProCS.ViewModels"
        xmlns:controls="clr-namespace:ShineProCS.Views.Controls"
        mc:Ignorable="d"
        Title="MaskWindow"
        Width="1920"
        Height="1080"
        d:DesignWidth="1920"
        d:DesignHeight="1080"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None">
    
    <Window.DataContext>
        <viewModel:MaskWindowViewModel />
    </Window.DataContext>
    
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Window.Background>
        <SolidColorBrush Opacity="0.00001" Color="Transparent" />
    </Window.Background>
    
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Controls/Draggable/DraggableResizableItem.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            
            <!-- 日志文本样式 -->
            <Style x:Key="LogTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, Courier New, monospace"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="LightGray"/>
            </Style>
            
            <!-- 方位文本样式 -->
            <Style x:Key="DirectionTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="34"/>
                <Setter Property="FontWeight" Value="DemiBold"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <AdornerDecorator ClipToBounds="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*" />
                <ColumnDefinition Width="23*" />
                <ColumnDefinition Width="250*" />
                <ColumnDefinition Width="1392*" />
                <ColumnDefinition Width="178*" />
                <ColumnDefinition Width="57*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1020*" />
                <RowDefinition Height="33*" />
                <RowDefinition Height="22*" />
                <RowDefinition Height="5*" />
            </Grid.RowDefinitions>

            <!-- 日志和状态 -->
            <Grid Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="5"
                  HorizontalAlignment="Left" VerticalAlignment="Bottom"
                  Height="213" Width="477">
                <Grid.RowDefinitions>
                    <RowDefinition Height="25" />
                    <RowDefinition Height="188" />
                </Grid.RowDefinitions>
                
                <!-- 日志框 - 使用可拖拽控件 -->
                <ContentControl x:Name="LogTextBoxWrapper"
                                Grid.Row="1"
                                Width="477" Height="188"
                                HorizontalAlignment="Left" VerticalAlignment="Top"
                                Style="{StaticResource OuterDraggableResizableItemStyle}">
                    <ContentControl.Effect>
                        <DropShadowEffect Opacity="0.4" BlurRadius="4" ShadowDepth="0" />
                    </ContentControl.Effect>
                    <RichTextBox x:Name="LogTextBox"
                                 Padding="0,5,0,0"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontFamily="Cascadia Mono, Consolas, Courier New, monospace"
                                 FontSize="12"
                                 IsHitTestVisible="False"
                                 VerticalScrollBarVisibility="Hidden"
                                 Opacity="{Binding Config.TextOpacity}"
                                 Visibility="{Binding Config.ShowLogBox, Converter={StaticResource BoolToVis}}">
                        <RichTextBox.Foreground>
                            <SolidColorBrush Color="LightGray" />
                        </RichTextBox.Foreground>
                        <FlowDocument>
                            <Paragraph>
                                <Run>[00:00:00 INF] ShineProCS 遮罩窗口已启动</Run>
                            </Paragraph>
                        </FlowDocument>
                    </RichTextBox>
                </ContentControl>

                <!-- 状态栏 - 使用可拖拽控件 -->
                <ContentControl x:Name="StatusWrapper"
                                Grid.Row="0"
                                Width="477" Height="24"
                                HorizontalAlignment="Left" VerticalAlignment="Top"
                                Style="{StaticResource OuterDraggableResizableItemStyle}">
                    <ContentControl.Effect>
                        <DropShadowEffect Opacity="0.4" BlurRadius="4" ShadowDepth="0" Color="LightGray" />
                    </ContentControl.Effect>
                    <ItemsControl ItemsSource="{Binding StatusList}"
                                  Visibility="{Binding Config.ShowStatus, Converter={StaticResource BoolToVis}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Margin="0,0,12,0"
                                           FontSize="12"
                                           Text="{Binding Name}">
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Setter Property="Foreground">
                                                <Setter.Value>
                                                    <SolidColorBrush Color="LightGray" Opacity="{Binding DataContext.Config.TextOpacity, RelativeSource={RelativeSource AncestorType=Window}}" />
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                                                    <Setter Property="Foreground">
                                                        <Setter.Value>
                                                            <SolidColorBrush Color="LightGreen" Opacity="{Binding DataContext.Config.TextOpacity, RelativeSource={RelativeSource AncestorType=Window}}" />
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ContentControl>
            </Grid>

            <!-- UID遮盖 -->
            <Rectangle Grid.Column="4"
                       Grid.Row="2"
                       Fill="White"
                       IsHitTestVisible="False"
                       Visibility="{Binding Config.UidCoverEnabled, Converter={StaticResource BoolToVis}}" />

            <!-- 方位指示（东南西北） -->
            <Viewbox Grid.Column="2" Grid.Row="0"
                     HorizontalAlignment="Left" VerticalAlignment="Top"
                     Visibility="{Binding Config.DirectionsEnabled, Converter={StaticResource BoolToVis}}">
                <Grid Width="250" Height="250">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.Effect>
                        <DropShadowEffect Opacity="0.4" BlurRadius="8" ShadowDepth="0" />
                    </Grid.Effect>
                    
                    <!-- 西 -->
                    <TextBlock Grid.Column="0" Grid.Row="2"
                               Style="{StaticResource DirectionTextStyle}"
                               Margin="-24,0,0,0"
                               Text="西">
                        <TextBlock.Foreground>
                            <SolidColorBrush Color="White" Opacity="{Binding Config.TextOpacity}" />
                        </TextBlock.Foreground>
                    </TextBlock>
                    
                    <!-- 南 -->
                    <TextBlock Grid.Column="2" Grid.Row="4"
                               Style="{StaticResource DirectionTextStyle}"
                               Margin="0,0,0,-24"
                               Text="南">
                        <TextBlock.Foreground>
                            <SolidColorBrush Color="White" Opacity="{Binding Config.TextOpacity}" />
                        </TextBlock.Foreground>
                    </TextBlock>
                    
                    <!-- 东 -->
                    <TextBlock Grid.Column="4" Grid.Row="2"
                               Style="{StaticResource DirectionTextStyle}"
                               Margin="0,0,-24,0"
                               Text="东">
                        <TextBlock.Foreground>
                            <SolidColorBrush Color="White" Opacity="{Binding Config.TextOpacity}" />
                        </TextBlock.Foreground>
                    </TextBlock>
                    
                    <!-- 北 -->
                    <TextBlock Grid.Column="2" Grid.Row="0"
                               Style="{StaticResource DirectionTextStyle}"
                               Margin="0,-24,0,0"
                               Text="北">
                        <TextBlock.Foreground>
                            <SolidColorBrush Color="White" Opacity="{Binding Config.TextOpacity}" />
                        </TextBlock.Foreground>
                    </TextBlock>
                </Grid>
            </Viewbox>

            <!-- FPS显示 -->
            <Border Margin="4,4,0,0"
                    Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3"
                    HorizontalAlignment="Left" VerticalAlignment="Top"
                    Background="#00000000"
                    CornerRadius="4"
                    Opacity="0.5"
                    Visibility="{Binding Config.ShowFps, Converter={StaticResource BoolToVis}}">
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontFamily="Consolas"
                           FontSize="16"
                           Text="{Binding Fps}">
                    <TextBlock.Foreground>
                        <SolidColorBrush Color="White" Opacity="{Binding Config.TextOpacity}" />
                    </TextBlock.Foreground>
                </TextBlock>
            </Border>
            
            <!-- Wine 兼容层支持 -->
            <Border Grid.Row="0" Grid.Column="0"
                    Grid.RowSpan="4" Grid.ColumnSpan="6"
                    IsHitTestVisible="False"
                    Visibility="{Binding IsRunningOnWine, Converter={StaticResource BoolToVis}}">
                <Border.Background>
                    <SolidColorBrush Color="Black" Opacity="0.07"/>
                </Border.Background>
            </Border>
        </Grid>
    </AdornerDecorator>
</Window>
