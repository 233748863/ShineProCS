<ui:FluentWindow x:Class="ShineProCS.Views.RegionPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="区域预览" Width="400" Height="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    
    <Grid Margin="16,40,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题和坐标信息 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="截图预览" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <TextBlock x:Name="RegionInfo" Text="区域: X=0, Y=0, W=0, H=0" FontSize="12" Foreground="#999"/>
        </StackPanel>
        
        <!-- 预览图像 -->
        <Border Grid.Row="1" Background="#333" CornerRadius="8" Padding="8">
            <Image x:Name="PreviewImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
        </Border>
        
        <!-- 微调按钮 -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="位置微调:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ui:Button Content="←" Width="32" Height="32" Margin="2" Click="MoveLeft_Click" ToolTip="左移1像素"/>
                <ui:Button Content="→" Width="32" Height="32" Margin="2" Click="MoveRight_Click" ToolTip="右移1像素"/>
                <ui:Button Content="↑" Width="32" Height="32" Margin="2" Click="MoveUp_Click" ToolTip="上移1像素"/>
                <ui:Button Content="↓" Width="32" Height="32" Margin="2" Click="MoveDown_Click" ToolTip="下移1像素"/>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="大小微调:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ui:Button Content="W-" Width="32" Height="32" Margin="2" Click="ShrinkWidth_Click" ToolTip="宽度-1"/>
                <ui:Button Content="W+" Width="32" Height="32" Margin="2" Click="ExpandWidth_Click" ToolTip="宽度+1"/>
                <ui:Button Content="H-" Width="32" Height="32" Margin="2" Click="ShrinkHeight_Click" ToolTip="高度-1"/>
                <ui:Button Content="H+" Width="32" Height="32" Margin="2" Click="ExpandHeight_Click" ToolTip="高度+1"/>
            </StackPanel>
        </Grid>
        
        <!-- 操作按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="刷新预览" Width="90" Margin="0,0,8,0" Click="Refresh_Click"/>
            <ui:Button Content="确定" Width="80" Margin="0,0,8,0" Click="Confirm_Click" Appearance="Primary"/>
            <ui:Button Content="取消" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
