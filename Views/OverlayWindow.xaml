<Window x:Class="ShineProCS.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShineProCS状态" 
        Width="220" Height="130"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Left="10" Top="10"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonDown="Window_MouseRightButtonDown">
    
    <Border x:Name="MainBorder" Background="#CC1E1E1E" CornerRadius="8" BorderBrush="#555" BorderThickness="1" Padding="12,8">
        <StackPanel>
            <DockPanel>
                <TextBlock Text="ShineProCS" FontWeight="Bold" Foreground="White" FontSize="12"/>
                <TextBlock x:Name="StatusText" Text="已停止" Foreground="#888" FontSize="11" HorizontalAlignment="Right"/>
            </DockPanel>
            
            <!-- 下一个技能显示 -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="下一技能:" Foreground="#888" FontSize="10"/>
                <TextBlock x:Name="NextSkillText" Text="无" Foreground="#FFD700" FontSize="10" FontWeight="SemiBold" Margin="4,0,0,0"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="执行:" Foreground="#888" FontSize="10"/>
                <TextBlock x:Name="CountText" Text="0" Foreground="#4CAF50" FontSize="10" Margin="4,0,12,0"/>
                <TextBlock Text="响应:" Foreground="#888" FontSize="10"/>
                <TextBlock x:Name="ResponseText" Text="0ms" Foreground="#2196F3" FontSize="10" Margin="4,0,0,0"/>
            </StackPanel>
            
            <!-- HP/MP 状态条 -->
            <Grid Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="HP" Foreground="#E57373" FontSize="9" VerticalAlignment="Center"/>
                <Border Grid.Column="1" Background="#333" CornerRadius="2" Height="6">
                    <Border x:Name="HpBar" Background="#E57373" CornerRadius="2" HorizontalAlignment="Left" Width="0"/>
                </Border>
                <TextBlock Grid.Column="3" Text="MP" Foreground="#64B5F6" FontSize="9" VerticalAlignment="Center"/>
                <Border Grid.Column="4" Background="#333" CornerRadius="2" Height="6">
                    <Border x:Name="MpBar" Background="#64B5F6" CornerRadius="2" HorizontalAlignment="Left" Width="0"/>
                </Border>
            </Grid>
        </StackPanel>
    </Border>
    
    <!-- 右键菜单 -->
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="MenuStartStop" Header="启动引擎" Click="MenuStartStop_Click"/>
            <MenuItem Header="暂停/恢复" Click="MenuPause_Click"/>
            <Separator/>
            <MenuItem x:Name="ProfileMenu" Header="切换方案"/>
            <Separator/>
            <MenuItem Header="透明度">
                <MenuItem Header="100%" Tag="1.0" Click="MenuOpacity_Click"/>
                <MenuItem Header="80%" Tag="0.8" Click="MenuOpacity_Click"/>
                <MenuItem Header="60%" Tag="0.6" Click="MenuOpacity_Click"/>
                <MenuItem Header="40%" Tag="0.4" Click="MenuOpacity_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="隐藏悬浮窗" Click="MenuHide_Click"/>
        </ContextMenu>
    </Window.ContextMenu>
</Window>
